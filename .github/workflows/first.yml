name: first workflow demo
on: workflow_dispatch
jobs:
  my_first_workflow_job:
    runs-on: Ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World!"
      - name: bit o' wisdom
        run: echo "never eat yellow snow"
      - name: Combo Execute
        run:
          echo "Hello World"
          echo "never eat yellow snow"
        
